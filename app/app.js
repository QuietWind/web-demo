import React, { Component } from 'react';
import ReactDOM from 'react-dom'

export class Index extends Component {
	render() {
		return (
			<div>
				todo
			</div>
		);
	}
}

ReactDOM.render(<Index />, document.getElementById('react-box'))